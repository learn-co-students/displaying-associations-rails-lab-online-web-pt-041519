<h1><%= @artist.name %></h1>

<h3><%= @artist.song_count %> <%= "song".pluralize(@artist.song_count) %></h3>

<ul>
  <% @artist.songs.each do |song| %>
    <li><%= link_to song.title, song_path %></li>
  <% end %>
</ul>