<h1>All Artists</h1>
<div id= "artist_names">
<%= @artists.each do |artist|%>
   <ul>
      <li><%= link_to artist.name, artist_path(artist)%> - <%= artist.song_count %> <%= "song".pluralize(artist.song_count) %> </li>
   </ul>
<%end%>
</div>